# Django GraphQL Boilerplate Environment Variables

# Security
SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (production example)
DB_NAME=django_graphql
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432